name: Restart Service

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Run commands on remote server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            cd /home/lucian/messagecrawlerbot
            git pull
            sudo docker stop msgcraw-bot
            sudo docker rm msgcraw-bot
            sudo docker build -t msgcrawbot .
            sudo docker run -d --restart unless-stopped --name msgcraw-bot msgcrawbot